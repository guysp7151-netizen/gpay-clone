// Central API configuration.
// Set EXPO_PUBLIC_API_URL in your deployment environment (Vercel, Render, etc.)
// For local dev this defaults to localhost.
export const API_BASE_URL =
  process.env.EXPO_PUBLIC_API_URL || 'http://localhost:3000';
